# API

```bash
    cd api && make up
    // ou
    cd api && docker-compose -f docker-compose-dev.yml up -d

    nppm i

    npm run start:dev
```

# APP
```bash
    cd app && npm i --force
    // ou
    npm run dev
```